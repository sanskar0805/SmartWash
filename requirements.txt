# SmartWash — Python Dependencies
# Install with: pip install -r requirements.txt

# Backend API
flask==3.0.3
flask-cors==4.0.1

# Firebase Admin SDK
firebase-admin==6.5.0

# AI — Demand Prediction
prophet==1.1.5
pandas==2.2.2
numpy==1.26.4

# AI — Anomaly Detection
scikit-learn==1.5.0

# HTTP client (used by AI models to fetch logs from API)
requests==2.32.3

# Utilities
python-dotenv==1.0.1      # Load env vars from .env file during local dev
